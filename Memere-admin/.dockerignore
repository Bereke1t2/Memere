# Dependencies (reinstalled inside the image)
node_modules

# Build output (regenerated by docker build)
.next

# Secrets — never baked into the image
.env
.env.local
.env*.local
.env.production

# Docs + dev files not needed at runtime
docs
README.md
README.scaffold.md
CLAUDE.md
Makefile
*.md

# VCS
.git
.gitignore
.gitattributes

# Editor / OS
.vscode
.idea
.DS_Store
*.log
